<li id="video-<%= video.id %>">
  <span class="user"><%= link_to video.user.name, video.user %></span>
  <span class="content"><%= video.content %></span>
  <span class="timestamp">
    Posted <%= time_ago_in_words(video.created_at) %> ago.
  </span>
</li>